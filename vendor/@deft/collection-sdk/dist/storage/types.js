// SDK-3: the pluggable credential-persistence contract (IMPLEMENTATION §3.4). The SDK never
// hardcodes paths or a storage mechanism — every adapter (memory, file, or a host-supplied one)
// implements this same shape.
export {};
//# sourceMappingURL=types.js.map